package controllers

import (
	"testing"
)

func TestPlatformServiceReconciler(t *testing.T) {
	// A simple unit test for the controller logic can go here.
	// Since we are mocking out actual k8s client, we just do a basic sanity check
	t.Log("Testing PlatformServiceReconciler")
}
